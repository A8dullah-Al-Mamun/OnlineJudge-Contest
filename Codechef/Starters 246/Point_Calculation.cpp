#include <iostream>

using namespace std;

int main()
{
    int A, B, C;

    cin >> A >> B >> C;

    int total_points = (A * 3) + (B * 1);

    cout << total_points << endl;

    return 0;
}